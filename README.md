# retrace-test-repo

Minimal repo used to test the Retrace GitHub Actions integration
(`.github/workflows/retrace-classify.yml`). The test in `tests/checkout.test.js`
fails on purpose so the workflow has something to classify.
...